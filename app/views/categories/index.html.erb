<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
         <% if user_signed_in?%>
        <td><%= link_to 'Show', category %></td>
        <%end%>
         <% if admin_user_signed_in?%>
        <td><%= link_to 'Edit', edit_category_path(category) %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>
 <% if !user_signed_in?%>
 <div class="text-justify">
  <h3 > Hay una gran cantidad de tiendas en la ciudad que uno solo conoce por datos y no hay un lugar objetivo dónde buscar y encontrar todas o una gran cantidad en un solo lugar. Por lo que se creo este buscador de tiendas, en el que si usted se registra puede encontrar la tienda que anda buscando.</h3>
 </div>
 <%end%>
<br>
<% @storextras.each do |storextra| %>
<table>
  <thead>
    <tr>
      <th>Foto</th>
      <th>Store</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%=image_tag storextra.avatar, style: 'height:150px; width:150px;' if storextra.avatar.attached? %></td> 
        <td><%= storextra.store.name %></td>
        <% if user_signed_in?%>
          <td><%= link_to 'Show', storextra %></td>
        <%end%>
      </tr>    
  </tbody>
</table>
<%end%>
<% if admin_user_signed_in?%>
  <%= link_to 'New Category', new_category_path %>
<%end%>
